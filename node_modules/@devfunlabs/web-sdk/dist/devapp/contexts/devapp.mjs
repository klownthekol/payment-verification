import { jsx as _jsx } from "react/jsx-runtime";
import "@devfunlabs/web-sdk/styles.css";
import { ConnectionProvider, WalletProvider } from "@solana/wallet-adapter-react";
import { createContext, useMemo } from "react";
import { WalletModalProvider } from "../wallet/index.mjs";
export const DevappContext = createContext(undefined);
export function DevappProvider({ rpcEndpoint, devbaseEndpoint, appId, children }) {
    const contextValue = useMemo(() => ({
        rpcEndpoint,
        devbaseEndpoint,
        appId,
    }), [rpcEndpoint, devbaseEndpoint, appId]);
    return (_jsx(DevappContext.Provider, { value: contextValue, children: _jsx(ConnectionProvider, { endpoint: rpcEndpoint, children: _jsx(WalletProvider, { wallets: [], autoConnect: true, children: _jsx(WalletModalProvider, { children: children }) }) }) }));
}
