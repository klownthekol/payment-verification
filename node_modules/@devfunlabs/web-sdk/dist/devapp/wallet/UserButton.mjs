import { jsx as _jsx } from "react/jsx-runtime";
import { BaseWalletMultiButton } from "./BaseWalletMultiButton.mjs";
const LABELS = {
    'change-wallet': 'Change wallet',
    'connecting': 'Connecting ...',
    'copy-address': 'Copy address',
    'copied': 'Copied',
    'disconnect': 'Disconnect',
    'has-wallet': 'Connect',
    'no-wallet': 'Connect Wallet',
};
export function UserButton({ labels = LABELS, ...props }) {
    return (_jsx(BaseWalletMultiButton, { ...props, labels: { ...LABELS, ...labels } }));
}
