"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HTTP = void 0;
const axios_1 = __importDefault(require("axios"));
const common_1 = require("./common.cjs");
exports.HTTP = {
    async get(url, options = undefined, isRetry = false) {
        const proxyURL = `https://proxy.dev.fun/?url=${encodeURIComponent(url)}`;
        try {
            return axios_1.default.get(proxyURL, options);
        }
        catch (error) {
            if (isRetry) {
                throw error;
            }
            await (0, common_1.sleep)(5000);
            return exports.HTTP.get(url, options, true);
        }
    },
};
